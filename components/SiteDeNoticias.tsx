import { Text, View } from "react-native";

const SiteDeNoticias = () => {

    try {
        
    } catch (error) {
        
    }

    return (
        <View>
       <Text>Aqui vai sair as ultimas noticias</Text>
        </View>
    );
}

export default SiteDeNoticias;